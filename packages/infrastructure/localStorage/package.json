{
  "name": "@misc-poc/infrastructure-localstorage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "sh -c 'if test -d src; then yarn exec tsc --build; else echo \"No src directory found, skipping build\"; fi'",
    "test": "jest",
    "lint": "sh -c 'if test -d src; then cd ../../../.. && yarn exec eslint packages/infrastructure/localStorage/src --ext .ts; else echo \"No src directory found, skipping lint\"; fi'",
    "typecheck": "sh -c 'if test -d src; then cd ../../.. && yarn tsc --noEmit --project packages/infrastructure/localStorage; else echo \"No src directory found, skipping typecheck\"; fi'",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@misc-poc/domain": "workspace:*",
    "@misc-poc/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^30.1.2",
    "jest-util": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
