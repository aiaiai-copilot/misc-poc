{
  "name": "@misc-poc/domain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "test -d src && tsc --build || echo 'No src directory found, skipping build'",
    "test": "jest",
    "lint": "test -d src && eslint src --ext .ts || echo 'No src directory found, skipping lint'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@misc-poc/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
